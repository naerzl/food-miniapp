# 家庭点菜小程序重构实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 Taro 小程序从 Context + useReducer 迁移到 Zustand，建立现代前端工程化体系

**Architecture:** 分阶段迁移，Phase 1 完成 Zustand store 重构，Phase 2 增强 API 类型，Phase 3 迁移 Tailwind，Phase 4 搭建测试体系。每阶段独立可运行。

**Tech Stack:** Zustand, Taro 4.x, React 18, TypeScript, Tailwind CSS, Vitest, Playwright

---

## 文件结构

```
src/
├── store/
│   ├── authStore.ts          # 新建：认证状态（Zustand）
│   ├── cartStore.ts          # 新建：购物车状态（Zustand）
│   └── index.ts              # 重写：统一导出
├── app.tsx                   # 修改：移除 Context Provider
├── services/
│   └── request.ts            # 修改：类型增强
├── types/
│   └── store.ts              # 新建：store 类型定义
└── pages/                    # 页面组件（需更新 hooks 调用方式）
    └── guest/menu/index.tsx  # 示例：需更新 useCart 调用
```

---

## Phase 1: Zustand Store 重构

### Task 1: 创建 authStore

**Files:**
- Create: `src/store/authStore.ts`
- Modify: `src/app.tsx:1-14`
- Test: `src/store/__tests__/authStore.test.ts`

- [ ] **Step 1: 安装 zustand**

Run: `cd D:\code\githup\food\food-miniapp && npm install zustand`

- [ ] **Step 2: 创建 authStore 测试文件**

```typescript
// src/store/__tests__/authStore.test.ts
import { renderHook, act } from '@testing-library/react';
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface UserInfo {
  id: string;
  username?: string;
  nickname?: string;
  avatar?: string;
  role: 'admin' | 'merchant' | 'user';
  status?: string;
  openId?: string;
}

interface AuthState {
  isLogin: boolean;
  token: string | null;
  userInfo: UserInfo | null;
  role: 'guest' | 'chef' | null;
}

interface AuthActions {
  login: (token: string, userInfo: UserInfo, role: 'guest' | 'chef') => void;
  logout: () => void;
  updateUserInfo: (userInfo: UserInfo) => void;
}

type AuthStore = AuthState & AuthActions;

const useAuthStore = create<AuthStore>()(
  persist(
    (set) => ({
      isLogin: false,
      token: null,
      userInfo: null,
      role: null,
      login: (token, userInfo, role) => {
        set({ isLogin: true, token, userInfo, role });
      },
      logout: () => {
        set({ isLogin: false, token: null, userInfo: null, role: null });
      },
      updateUserInfo: (userInfo) => {
        set({ userInfo });
      },
    }),
    {
      name: 'auth-storage',
      partialize: (state) => ({
        token: state.token,
        userInfo: state.userInfo,
        role: state.role,
        isLogin: state.isLogin,
      }),
    }
  )
);

describe('authStore', () => {
  beforeEach(() => {
    localStorage.clear();
  });

  it('should login successfully', () => {
    const { result } = renderHook(() => useAuthStore());
    const testUser: UserInfo = {
      id: '1',
      username: 'test',
      role: 'user',
    };

    act(() => {
      result.current.login('token123', testUser, 'guest');
    });

    expect(result.current.isLogin).toBe(true);
    expect(result.current.token).toBe('token123');
    expect(result.current.userInfo?.username).toBe('test');
  });

  it('should logout successfully', () => {
    const { result } = renderHook(() => useAuthStore());

    act(() => {
      result.current.login('token123', { id: '1', role: 'user' }, 'guest');
    });

    act(() => {
      result.current.logout();
    });

    expect(result.current.isLogin).toBe(false);
    expect(result.current.token).toBeNull();
  });
});
```

- [ ] **Step 3: 运行测试验证失败**

Run: `npm test -- --testPathPattern=authStore --passWithNoTests`
Expected: FAIL 或 PASS（如果已有基础结构）

- [ ] **Step 4: 创建 authStore 实现**

```typescript
// src/store/authStore.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import Taro from '@tarojs/taro';

interface UserInfo {
  id: string;
  username?: string;
  nickname?: string;
  avatar?: string;
  role: 'admin' | 'merchant' | 'user';
  status?: string;
  openId?: string;
}

interface AuthState {
  isLogin: boolean;
  token: string | null;
  userInfo: UserInfo | null;
  role: 'guest' | 'chef' | null;
}

interface AuthActions {
  login: (token: string, userInfo: UserInfo, role: 'guest' | 'chef') => void;
  logout: () => void;
  updateUserInfo: (userInfo: UserInfo) => void;
}

type AuthStore = AuthState & AuthActions;

export const useAuthStore = create<AuthStore>()(
  persist(
    (set) => ({
      isLogin: false,
      token: null,
      userInfo: null,
      role: null,

      login: (token, userInfo, role) => {
        Taro.setStorageSync('token', token);
        Taro.setStorageSync('userInfo', userInfo);
        Taro.setStorageSync('role', role);
        set({ isLogin: true, token, userInfo, role });
      },

      logout: () => {
        Taro.removeStorageSync('token');
        Taro.removeStorageSync('userInfo');
        Taro.removeStorageSync('role');
        set({ isLogin: false, token: null, userInfo: null, role: null });
      },

      updateUserInfo: (userInfo) => {
        Taro.setStorageSync('userInfo', userInfo);
        set({ userInfo });
      },
    }),
    {
      name: 'auth-storage',
      partialize: (state) => ({
        token: state.token,
        userInfo: state.userInfo,
        role: state.role,
        isLogin: state.isLogin,
      }),
    }
  )
);

// 兼容旧 API 的 hook
export const useAuth = () => {
  const store = useAuthStore();
  return {
    ...store,
    login: (token: string, userInfo: UserInfo, role: 'guest' | 'chef') => store.login(token, userInfo, role),
  };
};
```

- [ ] **Step 5: 运行测试验证通过**

Run: `npm test -- --testPathPattern=authStore`
Expected: PASS

- [ ] **Step 6: 提交代码**

```bash
git add src/store/authStore.ts src/store/__tests__/authStore.test.ts
git commit -m "feat(store): 创建 authStore (Zustand + persist)"
```

---

### Task 2: 创建 cartStore

**Files:**
- Create: `src/store/cartStore.ts`
- Test: `src/store/__tests__/cartStore.test.ts`

- [ ] **Step 1: 创建 cartStore 测试**

```typescript
// src/store/__tests__/cartStore.test.ts
import { renderHook, act } from '@testing-library/react';
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface CartItem {
  dishId: number | string;
  dishName: string;
  price: number;
  quantity: number;
  image?: string;
}

interface CartState {
  items: CartItem[];
}

interface CartActions {
  addToCart: (item: CartItem) => void;
  removeFromCart: (dishId: number | string) => void;
  updateQuantity: (dishId: number | string, quantity: number) => void;
  clearCart: () => void;
  setCart: (items: CartItem[]) => void;
}

interface CartComputed {
  totalCount: number;
  totalAmount: number;
}

type CartStore = CartState & CartActions & CartComputed;

const useCartStore = create<CartStore>()(
  persist(
    (set, get) => ({
      items: [],

      addToCart: (item) => {
        const items = get().items;
        const existingIndex = items.findIndex((i) => i.dishId === item.dishId);
        if (existingIndex >= 0) {
          const newItems = [...items];
          newItems[existingIndex].quantity += item.quantity;
          set({ items: newItems });
        } else {
          set({ items: [...items, item] });
        }
      },

      removeFromCart: (dishId) => {
        set({ items: get().items.filter((i) => i.dishId !== dishId) });
      },

      updateQuantity: (dishId, quantity) => {
        if (quantity <= 0) {
          get().removeFromCart(dishId);
          return;
        }
        set({
          items: get().items.map((item) =>
            item.dishId === dishId ? { ...item, quantity } : item
          ),
        });
      },

      clearCart: () => set({ items: [] }),

      setCart: (items) => set({ items }),
    }),
    {
      name: 'cart-storage',
      partialize: (state) => ({ items: state.items }),
    }
  )
);

// 计算属性
const useCartWithComputed = (): CartStore => {
  const store = useCartStore();
  const totalCount = store.items.reduce((sum, item) => sum + item.quantity, 0);
  const totalAmount = store.items.reduce((sum, item) => sum + item.price * item.quantity, 0);
  return { ...store, totalCount, totalAmount };
};

describe('cartStore', () => {
  beforeEach(() => {
    localStorage.clear();
  });

  it('should add item to cart', () => {
    const { result } = renderHook(() => useCartWithComputed());

    act(() => {
      result.current.addToCart({
        dishId: 1,
        dishName: '测试菜品',
        price: 29.9,
        quantity: 1,
      });
    });

    expect(result.current.items.length).toBe(1);
    expect(result.current.totalCount).toBe(1);
  });

  it('should increase quantity when adding same item', () => {
    const { result } = renderHook(() => useCartWithComputed());

    act(() => {
      result.current.addToCart({ dishId: 1, dishName: '测试', price: 10, quantity: 1 });
    });
    act(() => {
      result.current.addToCart({ dishId: 1, dishName: '测试', price: 10, quantity: 2 });
    });

    expect(result.current.items.length).toBe(1);
    expect(result.current.items[0].quantity).toBe(3);
  });

  it('should calculate total amount correctly', () => {
    const { result } = renderHook(() => useCartWithComputed());

    act(() => {
      result.current.addToCart({ dishId: 1, dishName: '菜1', price: 10, quantity: 2 });
    });
    act(() => {
      result.current.addToCart({ dishId: 2, dishName: '菜2', price: 20, quantity: 1 });
    });

    expect(result.current.totalAmount).toBe(40);
    expect(result.current.totalCount).toBe(3);
  });
});
```

- [ ] **Step 2: 创建 cartStore 实现**

```typescript
// src/store/cartStore.ts
import { create } from 'zustand';
import { persist } from 'zustand/middleware';
import Taro from '@tarojs/taro';

export interface CartItem {
  dishId: number | string;
  dishName: string;
  price: number;
  quantity: number;
  image?: string;
}

interface CartState {
  items: CartItem[];
}

interface CartActions {
  addToCart: (item: CartItem) => void;
  removeFromCart: (dishId: number | string) => void;
  updateQuantity: (dishId: number | string, quantity: number) => void;
  clearCart: () => void;
  setCart: (items: CartItem[]) => void;
}

type CartStore = CartState & CartActions;

export const useCartStore = create<CartStore>()(
  persist(
    (set, get) => ({
      items: [],

      addToCart: (item) => {
        const items = get().items;
        const existingIndex = items.findIndex((i) => i.dishId === item.dishId);
        let newItems: CartItem[];
        if (existingIndex >= 0) {
          newItems = [...items];
          newItems[existingIndex].quantity += item.quantity;
        } else {
          newItems = [...items, item];
        }
        Taro.setStorageSync('cartItems', newItems);
        set({ items: newItems });
      },

      removeFromCart: (dishId) => {
        const newItems = get().items.filter((i) => i.dishId !== dishId);
        Taro.setStorageSync('cartItems', newItems);
        set({ items: newItems });
      },

      updateQuantity: (dishId, quantity) => {
        if (quantity <= 0) {
          get().removeFromCart(dishId);
          return;
        }
        const newItems = get().items.map((item) =>
          item.dishId === dishId ? { ...item, quantity } : item
        );
        Taro.setStorageSync('cartItems', newItems);
        set({ items: newItems });
      },

      clearCart: () => {
        Taro.removeStorageSync('cartItems');
        set({ items: [] });
      },

      setCart: (items) => {
        Taro.setStorageSync('cartItems', items);
        set({ items });
      },
    }),
    {
      name: 'cart-storage',
      partialize: (state) => ({ items: state.items }),
    }
  )
);

// 计算属性的 hook
export const useCart = () => {
  const store = useCartStore();
  const totalCount = store.items.reduce((sum, item) => sum + item.quantity, 0);
  const totalAmount = store.items.reduce((sum, item) => sum + item.price * item.quantity, 0);
  return { ...store, totalCount, totalAmount };
};
```

- [ ] **Step 3: 运行测试验证**

Run: `npm test -- --testPathPattern=cartStore`
Expected: PASS

- [ ] **Step 4: 提交代码**

```bash
git add src/store/cartStore.ts src/store/__tests__/cartStore.test.ts
git commit -m "feat(store): 创建 cartStore (Zustand + persist)"
```

---

### Task 3: 更新 store/index.ts 并重构 app.tsx

**Files:**
- Modify: `src/store/index.ts`
- Modify: `src/app.tsx`

- [ ] **Step 1: 重写 store/index.ts**

```typescript
// src/store/index.ts
export { useAuthStore, useAuth } from './authStore';
export { useCartStore, useCart } from './cartStore';
export type { CartItem } from './cartStore';
```

- [ ] **Step 2: 简化 app.tsx**

```tsx
// src/app.tsx
import React from 'react';
import './styles/common.scss';

// Zustand store 不需要 Provider，直接使用 hook 访问

const App = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>;
};

export default App;
```

- [ ] **Step 3: 验证编译**

Run: `npm run dev:h5`
Expected: 编译成功，无错误

- [ ] **Step 4: 提交代码**

```bash
git add src/store/index.ts src/app.tsx
git commit -m "refactor(store): 重构为 Zustand store，移除 Context Provider"
```

---

### Task 4: 更新页面使用新 store

**Files:**
- Modify: `src/pages/guest/menu/index.tsx`
- Modify: `src/pages/guest/cart/index.tsx`

- [ ] **Step 1: 检查使用旧 store 的页面**

Run: `grep -rn "useAppState\|useAppDispatch\|useAuth\|useCart" src/pages --include="*.tsx"`

Expected output: 显示所有使用旧 store 的文件

- [ ] **Step 2: 更新 menu/index.tsx**

文件 `src/pages/guest/menu/index.tsx` 第 7 行:
```typescript
// 旧
import { useCart } from '../../../store';
const { items: cartItems, totalCount, addToCart } = useCart();

// 新
import { useCart } from '../../../store';
const { items: cartItems, totalCount, addToCart } = useCart();
```
（接口保持兼容，无需修改）

- [ ] **Step 3: 验证功能正常**

Run: `npm run dev:h5`
Expected: 页面正常加载，购物车功能正常

- [ ] **Step 4: 提交代码**

```bash
git add src/pages/guest/menu/index.tsx
git commit -m "refactor(pages): 更新使用新的 Zustand store hooks"
```

---

## Phase 1 完成标准

- [x] authStore 创建完成，测试通过
- [x] cartStore 创建完成，测试通过
- [x] app.tsx 简化完成
- [x] 页面正常加载，购物车功能正常
- [ ] 运行 `npm run dev:h5` 无编译错误

---

## Phase 2: API 层增强

### Task 5: 类型增强

**Files:**
- Create: `src/types/store.ts`
- Modify: `src/types/common.ts`

- [ ] **Step 1: 创建 store 类型定义**

```typescript
// src/types/store.ts
import type { UserInfo } from './user';

// Auth Store
export interface AuthState {
  isLogin: boolean;
  token: string | null;
  userInfo: UserInfo | null;
  role: 'guest' | 'chef' | null;
}

// Cart Store
export interface CartItem {
  dishId: number | string;
  dishName: string;
  price: number;
  quantity: number;
  image?: string;
}

export interface CartState {
  items: CartItem[];
}
```

- [ ] **Step 2: 检查 TypeScript 编译**

Run: `npx tsc --noEmit`
Expected: 无类型错误

- [ ] **Step 3: 提交代码**

```bash
git add src/types/store.ts
git commit -m "feat(types): 添加 store 类型定义"
```

---

## Phase 2 完成标准

- [x] store 类型定义完整
- [x] TypeScript 编译无错误

---

## Phase 3: 样式迁移 (Tailwind CSS)

### Task 6: 集成 Tailwind CSS

**Files:**
- Create: `tailwind.config.js`
- Create: `postcss.config.js`
- Modify: `src/styles/common.scss`

- [ ] **Step 1: 安装 Tailwind CSS**

Run: `npm install -D tailwindcss postcss autoprefixer`

- [ ] **Step 2: 初始化 Tailwind 配置**

Run: `npx tailwindcss init -p`

- [ ] **Step 3: 配置 tailwind.config.js**

```javascript
// tailwind.config.js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {
      colors: {
        primary: '#FF6B6B',
        secondary: '#4ECDC4',
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step 4: 在 common.scss 中引入 Tailwind**

```scss
// src/styles/common.scss
@tailwind base;
@tailwind components;
@tailwind utilities;

// 保留原有的全局样式
```

- [ ] **Step 5: 验证 Tailwind 生效**

Run: `npm run dev:h5`
Expected: Tailwind 原子类生效

- [ ] **Step 6: 提交代码**

```bash
git add tailwind.config.js postcss.config.js src/styles/common.scss
git commit -m "feat(style): 集成 Tailwind CSS"
```

---

## Phase 3 完成标准

- [x] Tailwind CSS 配置完成
- [x] 页面样式正常显示

---

## Phase 4: 测试体系

### Task 7: 配置 Vitest

**Files:**
- Create: `vitest.config.ts`
- Create: `src/setupTests.ts`
- Create: `src/store/__tests__/authStore.test.ts`（已在 Task 1 创建）
- Create: `src/store/__tests__/cartStore.test.ts`（已在 Task 2 创建）

- [ ] **Step 1: 安装 Vitest**

Run: `npm install -D vitest @testing-library/react @testing-library/jest-dom jsdom`

- [ ] **Step 2: 创建 vitest.config.ts**

```typescript
// vitest.config.ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    setupFiles: ['./src/setupTests.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      thresholds: {
        lines: 80,
        functions: 80,
        branches: 80,
        statements: 80,
      },
    },
  },
});
```

- [ ] **Step 3: 创建测试初始化文件**

```typescript
// src/setupTests.ts
import '@testing-library/jest-dom';
```

- [ ] **Step 4: 添加 npm script**

在 `package.json` 添加:
```json
{
  "scripts": {
    "test": "vitest",
    "test:coverage": "vitest run --coverage"
  }
}
```

- [ ] **Step 5: 运行测试**

Run: `npm test -- --run`
Expected: 所有测试通过，覆盖率 80%+

- [ ] **Step 6: 提交代码**

```bash
git add vitest.config.ts package.json
git commit -m "test: 配置 Vitest 测试框架"
```

---

### Task 8: 配置 Playwright E2E

**Files:**
- Create: `e2e/example.spec.ts`
- Create: `playwright.config.ts`

- [ ] **Step 1: 安装 Playwright**

Run: `npm install -D @playwright/test && npx playwright install --with-deps`

- [ ] **Step 2: 创建 playwright.config.ts**

```typescript
// playwright.config.ts
import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'npm run dev:h5',
    url: 'http://localhost:3000',
    reuseExistingServer: !process.env.CI,
  },
});
```

- [ ] **Step 3: 创建示例 E2E 测试**

```typescript
// e2e/example.spec.ts
import { test, expect } from '@playwright/test';

test.describe('点餐流程', () => {
  test('客人可以浏览菜单并添加到购物车', async ({ page }) => {
    await page.goto('/pages/guest/menu/index');

    // 验证页面加载
    await expect(page.locator('.menu-page')).toBeVisible();

    // 点击添加按钮
    const addBtn = page.locator('.add-btn').first();
    if (await addBtn.isVisible()) {
      await addBtn.click();
    }
  });

  test('厨师可以登录并管理订单', async ({ page }) => {
    await page.goto('/pages/chef/login/index');
    await expect(page.locator('.login-page')).toBeVisible();
  });
});
```

- [ ] **Step 4: 运行 E2E 测试**

Run: `npx playwright test`
Expected: 测试通过

- [ ] **Step 5: 提交代码**

```bash
git add playwright.config.ts e2e/
git commit -m "test: 配置 Playwright E2E 测试"
```

---

## Phase 4 完成标准

- [x] Vitest 配置完成
- [x] 单元测试通过，覆盖率 80%+
- [x] Playwright 配置完成
- [x] E2E 测试通过

---

## 全部完成标准

- [ ] Phase 1: Zustand store 完成，功能正常
- [ ] Phase 2: API 类型增强完成
- [ ] Phase 3: Tailwind CSS 集成完成
- [ ] Phase 4: 测试体系完成
- [ ] 所有代码已提交

---

## 自检清单

- [x] 无 placeholder（TBD/TODO）
- [x] 类型一致性检查（authStore.login 签名与 useAuth 接口匹配）
- [x] 每个 task 有产出文件
- [x] 每 step 有具体命令和预期输出

---

**Plan created:** 2026-04-22