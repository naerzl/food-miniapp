# 家庭点菜小程序客户端重构实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将小程序聚焦为纯客户端，移除厨师管理端，集成 Tailwind CSS，重设计 6 个客人端页面

**Architecture:** 分 5 个阶段：清理 chef 代码 → 集成 Tailwind → frontend-design 生成 UI 方案 → 逐页重写 → 验收测试。每阶段独立可提交。

**Tech Stack:** Taro 4.x, React 18, TypeScript, Zustand, Tailwind CSS

---

## 文件结构

```
src/
├── app.tsx                    # 修改：引用 tailwind.scss
├── app.config.ts              # 修改：移除 chef 分包
├── pages/
│   ├── index/                 # 修改：简化入口页，去除厨师选项
│   └── guest/
│       ├── menu/              # 重写：Tailwind 原子类
│       ├── cart/              # 重写：Tailwind 原子类
│       ├── confirm/           # 重写：Tailwind 原子类
│       ├── orders/            # 重写：Tailwind 原子类
│       ├── order-detail/      # 重写：Tailwind 原子类
│       └── profile/           # 重写：Tailwind 原子类
├── store/
│   ├── authStore.ts           # 修改：移除 role 字段
│   ├── cartStore.ts           # 不变
│   └── index.ts               # 不变
├── services/
│   ├── index.ts               # 修改：移除 statisticsApi 导出
│   └── statistics.ts          # 删除
├── styles/
│   └── tailwind.scss          # 新建：Tailwind 入口
├── tailwind.config.js         # 新建
└── postcss.config.js          # 修改
```

**删除的文件/目录：**
- `src/pages/chef/` 整个目录
- `src/services/statistics.ts`
- `src/styles/common.scss`
- `src/styles/theme.scss`

---

## Phase 1: 清理管理端

### Task 1: 删除 chef 分包，简化入口页和配置

**Files:**
- Delete: `src/pages/chef/` (entire directory)
- Modify: `src/app.config.ts`
- Modify: `src/pages/index/index.tsx`
- Modify: `src/services/index.ts`
- Delete: `src/services/statistics.ts`
- Modify: `src/store/authStore.ts`

- [ ] **Step 1: 删除 chef 分包目录**

Run: `Remove-Item -Recurse -Force "src/pages/chef"`

- [ ] **Step 2: 更新 app.config.ts，移除 chef 分包配置**

File: `src/app.config.ts`

```typescript
export default defineAppConfig({
  pages: [
    'pages/index/index',
    'pages/guest/menu/index',
    'pages/guest/cart/index',
    'pages/guest/confirm/index',
    'pages/guest/orders/index',
    'pages/guest/order-detail/index',
    'pages/guest/profile/index',
  ],
  tabBar: {
    color: '#999999',
    selectedColor: '#6BBF99',
    backgroundColor: '#FFFFFF',
    borderStyle: 'black',
    list: [
      {
        pagePath: 'pages/guest/menu/index',
        text: '菜单',
        iconPath: 'assets/tabbar/menu.png',
        selectedIconPath: 'assets/tabbar/menu-active.png',
      },
      {
        pagePath: 'pages/guest/cart/index',
        text: '购物车',
        iconPath: 'assets/tabbar/cart.png',
        selectedIconPath: 'assets/tabbar/cart-active.png',
      },
      {
        pagePath: 'pages/guest/orders/index',
        text: '订单',
        iconPath: 'assets/tabbar/order.png',
        selectedIconPath: 'assets/tabbar/order-active.png',
      },
      {
        pagePath: 'pages/guest/profile/index',
        text: '我的',
        iconPath: 'assets/tabbar/profile.png',
        selectedIconPath: 'assets/tabbar/profile-active.png',
      },
    ],
  },
  window: {
    backgroundTextStyle: 'light',
    navigationBarBackgroundColor: '#FFFFFF',
    navigationBarTitleText: '私厨点餐',
    navigationBarTextStyle: 'black',
    backgroundColor: '#F8F9FA',
  },
})
```

- [ ] **Step 3: 简化 index 入口页，去除厨师入口**

File: `src/pages/index/index.tsx`

```tsx
import React, { useEffect } from 'react'
import Taro from '@tarojs/taro'
import { View, Text } from '@tarojs/components'
import { AtButton } from 'taro-ui'
import { useAuth } from '../../store'
import './index.scss'

const IndexPage: React.FC = () => {
  const { isLogin } = useAuth()

  useEffect(() => {
    if (isLogin) {
      Taro.switchTab({ url: '/pages/guest/menu/index' })
    }
  }, [isLogin])

  const handleEnter = () => {
    Taro.switchTab({ url: '/pages/guest/menu/index' })
  }

  return (
    <View className="index-page">
      <View className="hero-section">
        <View className="logo-section">
          <View className="logo-icon">🍽️</View>
          <Text className="app-name">私厨点餐助手</Text>
          <Text className="app-slogan">家庭私厨，美味随时享</Text>
        </View>
      </View>

      <View className="action-section">
        <AtButton
          type="primary"
          className="guest-btn"
          onClick={handleEnter}
        >
          进入点餐
        </AtButton>
      </View>

      <View className="footer-section">
        <Text className="footer-text">让家庭用餐更简单、更温馨</Text>
      </View>
    </View>
  )
}

export default IndexPage
```

- [ ] **Step 4: 简化 authStore，移除 role 字段**

File: `src/store/authStore.ts`

```typescript
import { create } from 'zustand'
import { persist } from 'zustand/middleware'
import Taro from '@tarojs/taro'

interface UserInfo {
  id: string
  username?: string
  nickname?: string
  avatar?: string
  role: 'admin' | 'merchant' | 'user'
  status?: string
  openId?: string
}

interface AuthState {
  isLogin: boolean
  token: string | null
  userInfo: UserInfo | null
}

interface AuthActions {
  login: (token: string, userInfo: UserInfo) => void
  logout: () => void
  updateUserInfo: (userInfo: UserInfo) => void
}

type AuthStore = AuthState & AuthActions

export const useAuthStore = create<AuthStore>()(
  persist(
    (set) => ({
      isLogin: false,
      token: null,
      userInfo: null,

      login: (token, userInfo) => {
        Taro.setStorageSync('token', token)
        Taro.setStorageSync('userInfo', userInfo)
        set({ isLogin: true, token, userInfo })
      },

      logout: () => {
        Taro.removeStorageSync('token')
        Taro.removeStorageSync('userInfo')
        set({ isLogin: false, token: null, userInfo: null })
      },

      updateUserInfo: (userInfo) => {
        Taro.setStorageSync('userInfo', userInfo)
        set({ userInfo })
      },
    }),
    {
      name: 'auth-storage',
      partialize: (state) => ({
        token: state.token,
        userInfo: state.userInfo,
        isLogin: state.isLogin,
      }),
    }
  )
)

export const useAuth = () => {
  const store = useAuthStore()
  return {
    ...store,
    login: (token: string, userInfo: UserInfo) => store.login(token, userInfo),
  }
}
```

- [ ] **Step 5: 更新 services/index.ts，移除 statisticsApi 导出**

File: `src/services/index.ts`

```typescript
export { authApi } from './auth'
export { userApi } from './user'
export { categoryApi } from './category'
export { dishApi } from './dish'
export { orderApi } from './order'
```

- [ ] **Step 6: 删除 statistics.ts**

Run: `Remove-Item "src/services/statistics.ts"`

- [ ] **Step 7: 更新 profile 页，移除 statistics 和 chef 入口**

File: `src/pages/guest/profile/index.tsx`

```tsx
import React, { useState, useEffect, useCallback } from 'react'
import Taro, { useDidShow } from '@tarojs/taro'
import { View, Text, Image } from '@tarojs/components'
import { AtList, AtListItem, AtButton, AtActivityIndicator } from 'taro-ui'
import { useAuth, useCart } from '../../../store'
import { userApi, authApi } from '../../../services'
import { User } from '../../../../types/api'
import './index.scss'

const ProfilePage: React.FC = () => {
  const { isLogin, userInfo, login, logout } = useAuth()
  const { clearCart } = useCart()
  const [loading, setLoading] = useState(false)
  const [userDetail, setUserDetail] = useState<User | null>(null)

  const loadUserData = useCallback(async () => {
    if (!isLogin) return

    try {
      setLoading(true)
      const profileRes = await userApi.getProfile()
      setUserDetail(profileRes)
    } catch (error) {
      console.error('加载用户数据失败:', error)
    } finally {
      setLoading(false)
    }
  }, [isLogin])

  useEffect(() => {
    loadUserData()
  }, [loadUserData])

  useDidShow(() => {
    loadUserData()
  })

  const handleWechatLogin = async () => {
    try {
      setLoading(true)

      const { code } = await Taro.login({
        provider: 'weixin',
      })

      const res = await authApi.wechatLogin({ code })

      login(res.accessToken, res.user)

      Taro.showToast({
        title: '登录成功',
        icon: 'success',
      })

      loadUserData()
    } catch (error) {
      console.error('登录失败:', error)
      Taro.showToast({
        title: '登录失败',
        icon: 'none',
      })
    } finally {
      setLoading(false)
    }
  }

  const handleLogout = () => {
    Taro.showModal({
      title: '提示',
      content: '确定要退出登录吗？',
      success: (res) => {
        if (res.confirm) {
          logout()
          clearCart()
          Taro.showToast({
            title: '已退出登录',
            icon: 'success',
          })
        }
      },
    })
  }

  if (!isLogin) {
    return (
      <View className="profile-page">
        <View className="login-section">
          <View className="login-icon">👋</View>
          <Text className="login-title">欢迎使用私厨点餐</Text>
          <Text className="login-desc">登录后可查看订单、享受更多服务</Text>
          <AtButton
            type="primary"
            className="wechat-login-btn"
            loading={loading}
            onClick={handleWechatLogin}
          >
            微信一键登录
          </AtButton>
        </View>
      </View>
    )
  }

  return (
    <View className="profile-page">
      <View className="user-card">
        <Image
          className="user-avatar"
          src={userDetail?.avatar || 'https://via.placeholder.com/100?text=Avatar'}
          mode="aspectFill"
        />
        <View className="user-info">
          <Text className="user-name">
            {userDetail?.nickname || userDetail?.username || '微信用户'}
          </Text>
        </View>
      </View>

      {loading && (
        <View className="loading-indicator">
          <AtActivityIndicator size="small" />
        </View>
      )}

      <View className="menu-section">
        <AtList>
          <AtListItem
            title="我的订单"
            arrow="right"
            iconInfo={{ size: 24, color: '#FF7A45', value: 'shopping-bag' }}
            onClick={() => Taro.switchTab({ url: '/pages/guest/orders/index' })}
          />
          <AtListItem
            title="购物车"
            arrow="right"
            iconInfo={{ size: 24, color: '#FF7A45', value: 'shopping-cart' }}
            onClick={() => Taro.switchTab({ url: '/pages/guest/cart/index' })}
          />
          <AtListItem
            title="菜单浏览"
            arrow="right"
            iconInfo={{ size: 24, color: '#FF7A45', value: 'menu' }}
            onClick={() => Taro.switchTab({ url: '/pages/guest/menu/index' })}
          />
        </AtList>
      </View>

      <View className="menu-section">
        <AtList>
          <AtListItem
            title="联系私厨"
            arrow="right"
            iconInfo={{ size: 24, color: '#1677FF', value: 'phone' }}
            onClick={() => Taro.showToast({ title: '功能开发中', icon: 'none' })}
          />
          <AtListItem
            title="意见反馈"
            arrow="right"
            iconInfo={{ size: 24, color: '#52C41A', value: 'message' }}
            onClick={() => Taro.showToast({ title: '功能开发中', icon: 'none' })}
          />
        </AtList>
      </View>

      <View className="logout-section">
        <AtButton className="logout-btn" onClick={handleLogout}>
          退出登录
        </AtButton>
      </View>
    </View>
  )
}

export default ProfilePage
```

- [ ] **Step 8: 更新 confirm 页中的 login 调用（去掉 role 参数）**

File: `src/pages/guest/confirm/index.tsx` — 无需改动，confirm 页不直接调用 login

- [ ] **Step 9: 验证编译通过**

Run: `npm run build:weapp`
Expected: 编译成功，无报错

- [ ] **Step 10: 提交代码**

```bash
git add -A
git commit -m "refactor: 移除厨师管理端，简化 authStore，清理 statistics"
```

---

## Phase 2: 集成 Tailwind CSS

### Task 2: 安装和配置 Tailwind CSS

**Files:**
- Create: `tailwind.config.js`
- Create: `postcss.config.js`
- Create: `src/styles/tailwind.scss`
- Modify: `src/app.tsx`

- [ ] **Step 1: 安装 Tailwind CSS**

Run: `npm install -D tailwindcss@3 autoprefixer`

- [ ] **Step 2: 创建 tailwind.config.js**

File: `tailwind.config.js`

```javascript
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'],
  theme: {
    extend: {},
  },
  corePlugins: {
    preflight: false,
  },
}
```

- [ ] **Step 3: 创建 postcss.config.js**

File: `postcss.config.js`

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

- [ ] **Step 4: 创建 Tailwind 入口样式文件**

File: `src/styles/tailwind.scss`

```scss
@tailwind base;
@tailwind components;
@tailwind utilities;
```

- [ ] **Step 5: 更新 app.tsx 引用新的样式文件**

File: `src/app.tsx`

```tsx
import React from 'react'
import './styles/tailwind.scss'

const App = ({ children }: { children: React.ReactNode }) => {
  return <>{children}</>
}

export default App
```

- [ ] **Step 6: 验证 Tailwind 编译**

Run: `npm run build:weapp`
Expected: 编译成功，CSS 中包含 Tailwind 原子类

- [ ] **Step 7: 提交**

```bash
git add tailwind.config.js postcss.config.js src/styles/tailwind.scss src/app.tsx
git commit -m "feat: 集成 Tailwind CSS"
```

---

## Phase 3: frontend-design 生成 UI 方案

### Task 3: 使用 frontend-design 技能生成 6 个页面的 UI 设计

> **注意：** 此任务使用 `/frontend-design` 技能逐页生成 UI 设计。设计方向由 frontend-design 根据最佳实践自由发挥。

- [ ] **Step 1: 为菜单页设计 UI**

调用 frontend-design 技能，输入菜单页的需求描述：
- 顶部横向滚动分类标签
- 菜品卡片（图片 + 菜名 + 描述 + 价格 + 加购按钮）
- 右下角悬浮购物车按钮（数量角标）
- 加载骨架屏 / 空状态 / 售罄灰显
- 使用 Tailwind 类名

- [ ] **Step 2: 为购物车页设计 UI**

调用 frontend-design 技能，输入购物车页需求：
- 商品列表（图片、名称、单价、数量调节器 +/-、小计）
- 底部结算栏（合计金额 + 去下单按钮）
- 空状态插画 + 引导按钮
- 左滑删除、数量为 0 自动移除

- [ ] **Step 3: 为确认下单页设计 UI**

调用 frontend-design 技能，输入确认下单页需求：
- 订单明细（只读）+ 备注输入框
- 金额汇总 + 确认下单按钮
- 可返回购物车

- [ ] **Step 4: 为订单列表页设计 UI**

调用 frontend-design 技能，输入订单列表页需求：
- Tab 筛选（全部/待支付/待制作/制作中/已完成）
- 订单卡片（订单号、状态标签、菜品摘要、金额、时间）
- 下拉刷新 + 空状态

- [ ] **Step 5: 为订单详情页设计 UI**

调用 frontend-design 技能，输入订单详情页需求：
- 状态进度展示 + 状态图标 + 状态描述
- 菜品列表 + 金额汇总
- 订单信息（编号、时间、备注）
- 操作按钮（取消订单/立即支付/再来一单）

- [ ] **Step 6: 为个人中心页设计 UI**

调用 frontend-design 技能，输入个人中心页需求：
- 用户头像 + 昵称
- 功能列表入口（我的订单、购物车、菜单）
- 退出登录

---

## Phase 4: 逐页重写 UI

### Task 4: 重写菜单页 UI

**File:** `src/pages/guest/menu/index.tsx` + `src/pages/guest/menu/index.scss`

- [ ] **Step 1: 根据 frontend-design 生成的 UI 方案，用 Tailwind 类名重写菜单页**

替换现有 SCSS 类名为 Tailwind 原子类，同时删除页面级 SCSS 文件。使用 Taro 组件 + Tailwind 类名。

- [ ] **Step 2: 验证菜单页 UI 正常**

Run: `npm run dev:weapp`

- [ ] **Step 3: 提交**

```bash
git add src/pages/guest/menu/
git commit -m "refactor(ui): 重写菜单页为 Tailwind 样式"
```

### Task 5: 重写购物车页 UI

**File:** `src/pages/guest/cart/index.tsx` + `src/pages/guest/cart/index.scss`

- [ ] **Step 1: 根据 frontend-design 生成的 UI 方案，用 Tailwind 类名重写购物车页**
- [ ] **Step 2: 验证购物车 UI 正常**
- [ ] **Step 3: 提交**

### Task 6: 重写确认下单页 UI

**File:** `src/pages/guest/confirm/index.tsx` + `src/pages/guest/confirm/index.scss`

- [ ] **Step 1: 根据 frontend-design 生成的 UI 方案，用 Tailwind 类名重写确认下单页**
- [ ] **Step 2: 验证确认下单页 UI 正常**
- [ ] **Step 3: 提交**

### Task 7: 重写订单列表页 UI

**File:** `src/pages/guest/orders/index.tsx` + `src/pages/guest/orders/index.scss`

- [ ] **Step 1: 根据 frontend-design 生成的 UI 方案，用 Tailwind 类名重写订单列表页**
- [ ] **Step 2: 验证订单列表 UI 正常**
- [ ] **Step 3: 提交**

### Task 8: 重写订单详情页 UI

**File:** `src/pages/guest/order-detail/index.tsx` + `src/pages/guest/order-detail/index.scss`

- [ ] **Step 1: 根据 frontend-design 生成的 UI 方案，用 Tailwind 类名重写订单详情页**
- [ ] **Step 2: 验证订单详情 UI 正常**
- [ ] **Step 3: 提交**

### Task 9: 重写个人中心页 UI

**File:** `src/pages/guest/profile/index.tsx` + `src/pages/guest/profile/index.scss`

- [ ] **Step 1: 根据 frontend-design 生成的 UI 方案，用 Tailwind 类名重写个人中心页**
- [ ] **Step 2: 验证个人中心 UI 正常**
- [ ] **Step 3: 提交**

---

## Phase 5: 验收与清理

### Task 10: 清理和最终验证

- [ ] **Step 1: 删除残留的 SCSS 文件**

删除各页面目录下的 `index.scss` 文件（如果仍存在），以及全局 `common.scss`、`theme.scss`。

- [ ] **Step 2: 移除 taro-ui 依赖（如不再使用）**

如果 6 个页面都不再使用 taro-ui 组件（AtButton、AtList 等），则：
Run: `npm uninstall taro-ui`

- [ ] **Step 3: 全流程验证**

Run: `npm run build:weapp`
Expected: 编译成功，无警告

手动验证流程：
1. 打开小程序 → 进入菜单页 → 浏览菜品
2. 点击加购 → 购物车数量更新
3. 进入购物车 → 调整数量 → 去结算
4. 确认下单 → 提交成功 → 跳转订单详情
5. 查看订单列表 → 点击订单 → 查看详情
6. 进入个人中心 → 登录/退出

- [ ] **Step 4: 提交代码**

```bash
git add -A
git commit -m "chore: 清理残留文件，移除 taro-ui，最终验收"
```

---

## 全部完成标准

- [ ] chef 分包及相关代码完全移除
- [ ] authStore 不再包含 role 字段
- [ ] Tailwind CSS 集成完成
- [ ] 6 个页面使用 Tailwind 类名重写，删除 index.scss
- [ ] 菜单浏览 → 购物车 → 下单全流程正常
- [ ] `npm run build:weapp` 编译通过无错误
- [ ] taro-ui 依赖已移除（如页面不再使用）

---

## 自检清单

- [x] 无 placeholder（TBD/TODO）
- [x] Phase 1 包含完整的 authStore 简化逻辑
- [x] index.tsx 不再引用 `role` 字段
- [x] profile.tsx 不再使用 `statisticsApi`
- [x] services/index.ts 不再导出 `statisticsApi`
- [x] app.config.ts 不再包含 chef 分包
- [x] 所有 store 使用方 login 调用不再传 role 参数
- [x] Phase 3 由 frontend-design 逐页生成 UI 方案
- [x] Phase 4 根据 frontend-design 输出逐页实现

---

**Plan created:** 2026-05-25
